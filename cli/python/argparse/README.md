# Netbox CLI for Scaleway

## Usage

```
usage: main.py [-h] [-p params]

Netbox API for Scaleway

optional arguments:
  -h, --help            show this help message and exit
  -p params, --params params
                        params for the request, formatted as JSON
```
